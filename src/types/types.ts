enum Choices {
    ROCK = 'rock',
    PAPER = 'paper',
    SCISSORS = 'scissors',
}

enum Results {
    PLAYER = 'player',
    COMPUTER = 'computer',
    DRAW = 'draw',
}

enum ResultsMessages {
    WIN = 'You win!',
    LOSE = 'You lose!',
    DRAW = "It's a draw!",
}

export { Choices, ResultsMessages, Results }
